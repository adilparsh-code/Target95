export const metadata = {
  title: "Dashboard | Target95+",
  description: "Track your Computer Science learning progress, practice activity, streaks, and mock tests in Target95+.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function DashboardLayout({ children }) {
  return children;
}
