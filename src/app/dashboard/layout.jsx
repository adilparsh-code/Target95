export const metadata = {
  title: "Dashboard | Target95+",
  description: "Track your Target95+ learning progress, practice activity and upcoming mock tests.",
};

export default function DashboardLayout({ children }) {
  return children;
}
