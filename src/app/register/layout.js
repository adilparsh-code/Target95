export const metadata = {
  title: "Create Account | Target95+",
  description: "Create a Target95+ account and start preparing for ICSE and ISC Computer Science exams.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function RegisterLayout({ children }) {
  return children;
}
